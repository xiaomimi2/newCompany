define(function (require, exports, module) {
	var tpl = require('../../common/templates.js')
	var util = require('../../common/util.js')
	var ajax = util['ajax']
	$('.listshow').eq(0).on('click', function () {
		// ajax('POST','')
		// tpl['claim']({})
	})
})